---
# NOTE: Plain-text Secret for the school assignment.
# In a real cluster this MUST be replaced with SealedSecret / ExternalSecret
# and rotated automatically. See THREAT_MODEL.md → "Credential leak".
apiVersion: v1
kind: Secret
metadata:
  name: inference-s3
  namespace: mlops-production
type: Opaque
stringData:
  access_key_id: "minio"
  secret_access_key: "minio123"
